{
  "name": "medknowledge-server",
  "version": "0.1.1",
  "type": "module",
  "scripts": {
    "dev": "node -r dotenv/config server.js",
    "start": "node -r dotenv/config server.js",
    "lint": "eslint . --ext .js,.cjs,.mjs --max-warnings=0",
    "test": "echo \"No tests yet\" && exit 0",
    "seed:endo": "node scripts/seedEndoPlaceholders.mjs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "mongoose": "^7.8.7",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "eslint": "^8.57.1"
  }
}

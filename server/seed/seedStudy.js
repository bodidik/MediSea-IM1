import mongoose from 'mongoose';import 'dotenv/config';import StudyProgram from '../models/StudyProgram.js';const uri=process.env.MONGODB_URI||'mongodb://localhost:27017/medknowledge';await mongoose.connect(uri);const days=Array.from({length:120},(_,i)=>({dayNo:i+1,title:`Gün ${i+1} — Örnek`,topics:['FMF'],contentRefs:[]}));await StudyProgram.deleteMany({code:'YDUS-120'});await StudyProgram.create({code:'YDUS-120',title:'YDUS 120 Gün',lang:'tr',days});console.log('Seeded YDUS-120');process.exit(0);
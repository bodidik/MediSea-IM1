import { Router } from 'express';const router=Router();const clients=new Set();router.get('/notifications',(req,res)=>{res.setHeader('Content-Type','text/event-stream');res.setHeader('Cache-Control','no-cache');res.setHeader('Connection','keep-alive');const c={res};clients.add(c);res.write(`event: hello\n`);res.write(`data: ${JSON.stringify({ok:true})}\n\n`);req.on('close',()=>clients.delete(c));});export default router;